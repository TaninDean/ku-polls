language: python

python: "3.6"

# don't clone more than necessary
git:
  depth: 1

# Install dependencies
install:
  - pip install -r requirements.txt

env:
- CODECOV_TOKEN='160ac283-4e59-46cb-bdfa-100a9b3e782f'
  
# script to run tests. Script can have many commands, one per line.
script: 
  - python -m unittest discover -p "*_tests.py"
  - coverage run -m unittest discover -p "*_tests.py"

after_script:
  - bash <(curl https://codecov.io/bash) -t <'160ac283-4e59-46cb-bdfa-100a9b3e782f'>
  